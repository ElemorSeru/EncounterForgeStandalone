<Window x:Class="EncounterForgeStandalone.Views.ResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:EncounterForgeStandalone.Converters"
        Style="{StaticResource AppWindowStyle}"
        Icon="/Assets/app.ico"
        Title="Generated Encounter"
        Width="560" Height="620"
        ResizeMode="CanResize"
        MinHeight="400" MinWidth="460"
        WindowStartupLocation="CenterOwner">

  <Window.Resources>
    <conv:OutcomeColorConverter x:Key="OutcomeColor" />
    <conv:BoolToVisibilityConverter x:Key="BoolVis" />
    <conv:InverseBoolConverter x:Key="InvBool" />
  </Window.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="52" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Header -->
    <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
      <Grid Margin="20,0">
        <TextBlock Text="GENERATED ENCOUNTER" Foreground="{StaticResource AccentBrush}"
                   FontSize="14" FontWeight="Bold" VerticalAlignment="Center" />
        <Border Grid.Column="1" CornerRadius="4" Padding="10,4" VerticalAlignment="Center"
                HorizontalAlignment="Right"
                Background="{Binding OutcomeKey, Converter={StaticResource OutcomeColor}}">
          <TextBlock Text="{Binding OutcomeText}" FontWeight="SemiBold" Foreground="White" FontSize="12" />
        </Border>
      </Grid>
    </Border>

    <!-- Body -->
    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0">
      <StackPanel Margin="20,16">

        <!-- Creature cards -->
        <ItemsControl ItemsSource="{Binding Creatures}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Border Style="{StaticResource CardStyle}" Margin="0,0,0,10">
                <Grid>
                  <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                  </Grid.RowDefinitions>
                  <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                      <TextBlock Text="{Binding Name}" FontSize="17" FontWeight="SemiBold"
                                 Foreground="{StaticResource TextPrimaryBrush}" />
                      <TextBlock Text="{Binding Cr}" FontSize="12"
                                 Foreground="{StaticResource AccentGoldBrush}" />
                    </StackPanel>
                  </Grid>
                  <Border Grid.Row="1" Style="{StaticResource DividerStyle}" Margin="0,6" />
                  <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                      <TextBlock Text="{Binding Ac}" FontSize="13" FontWeight="SemiBold"
                                 Foreground="{StaticResource TextPrimaryBrush}" />
                      <TextBlock Text="Armor Class" FontSize="10"
                                 Foreground="{StaticResource TextMutedBrush}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                      <TextBlock Text="{Binding Hp}" FontSize="13" FontWeight="SemiBold"
                                 Foreground="{StaticResource TextPrimaryBrush}" />
                      <TextBlock Text="Hit Points" FontSize="10"
                                 Foreground="{StaticResource TextMutedBrush}" />
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                      <TextBlock Text="{Binding Dpr}" FontSize="13" FontWeight="SemiBold"
                                 Foreground="{StaticResource TextPrimaryBrush}" />
                      <TextBlock Text="Avg DPR" FontSize="10"
                                 Foreground="{StaticResource TextMutedBrush}" />
                    </StackPanel>
                  </Grid>
                </Grid>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Summary -->
        <Border Style="{StaticResource CardStyle}" Background="{StaticResource ElevatedBrush}">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
              <TextBlock Text="PARTY" Style="{StaticResource SectionLabelStyle}" FontSize="10" />
              <TextBlock Text="{Binding PartyDprHp}" FontSize="12" FontWeight="SemiBold"
                         Foreground="{StaticResource TextPrimaryBrush}" />
            </StackPanel>
            <StackPanel Grid.Column="1">
              <TextBlock Text="ENCOUNTER" Style="{StaticResource SectionLabelStyle}" FontSize="10" />
              <TextBlock Text="{Binding GroupDprHp}" FontSize="12" FontWeight="SemiBold"
                         Foreground="{StaticResource TextPrimaryBrush}" />
            </StackPanel>
            <StackPanel Grid.Column="2">
              <TextBlock Text="ROUNDS" Style="{StaticResource SectionLabelStyle}" FontSize="10" />
              <TextBlock Text="{Binding RoundsText}" FontSize="12" FontWeight="SemiBold"
                         Foreground="{StaticResource TextPrimaryBrush}" />
              <TextBlock Text="defeat / drain" FontSize="10"
                         Foreground="{StaticResource TextMutedBrush}" />
            </StackPanel>
          </Grid>
        </Border>

      </StackPanel>
    </ScrollViewer>

    <!-- Footer -->
    <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0"
            Padding="20,12">
      <Grid>
        <TextBlock Text="Export to PDF to get full stat blocks for each creature."
                   Foreground="{StaticResource TextMutedBrush}" FontSize="11"
                   VerticalAlignment="Center" />
        <Button Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right"
                Command="{Binding ExportPdfCommand}"
                IsEnabled="{Binding IsExporting, Converter={StaticResource InvBool}}">
          <TextBlock>
            <Run Text="Export" />
            <Run Text=" PDF" />
          </TextBlock>
        </Button>
      </Grid>
    </Border>
  </Grid>
</Window>
